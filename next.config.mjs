/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["source.unsplash.com", "cdn.pixabay.com", 'images.unsplash.com', 'plus.unsplash.com'],
  },
};

export default nextConfig;
